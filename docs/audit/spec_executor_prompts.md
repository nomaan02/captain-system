# Spec Executor Prompts — Phase 2

Generated: 2026-04-08 by Orchestrator
Prompts: SPEC-EXEC-01 through SPEC-EXEC-07
Output: `docs/audit/spec_reference.md` (§1–13) + `docs/audit/master_gap_analysis.md`

---

## How to Use

1. Open a fresh Claude Code terminal in the `captain-system/` directory
2. Copy the entire prompt block for the session (between the `---` dividers)
3. Paste it as your first message
4. For sessions 02–06: prepend the passover block from the previous session's spec_reference.md output
5. After each session: return to Orchestrator to track progress

---

## SPEC-EXEC-01 — Online Pipeline Spec (Core)

---

You are extracting spec content for the Captain System consolidated spec reference. Session 1 of 7.

**Your role:** Read the spec documents listed below, extract structured data, and write it to `docs/audit/spec_reference.md`. You are creating this file from scratch.

**Source priority:** V3 repo specs SUPERSEDE Obsidian vault originals where content conflicts. When both cover the same topic, prefer the V3 version. Include Obsidian-only content that is absent from V3 docs.

### Documents to Read

**PRIMARY (V3 repo — authoritative):**
- `docs/AIM-Specs/new-aim-specs/Program3_Online.md` (1,756 lines) — Full V3 Online pipeline spec

**SUPPLEMENTARY (Obsidian vault — original Isaac specs):**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/33_P3_Online_Full_Pseudocode.md` (475 lines) — Online pseudocode
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/30_P1_Consolidated_Config.md` (89 lines) — P1 session/config definitions

### What to Extract

**Section 1 — Session Definitions**

Extract ALL of the following:
- Session type enum: NY=1, LON=2, APAC=3 (or whatever the spec defines)
- `session_match()` logic: how assets map to sessions
- Per-session open times (ET timezone): exact times for NY open, LON open, APAC open
- Per-session asset mapping: which of the 10 active assets trade in which session(s)
- Opening Range (OR) parameters: duration, calculation method
- Any session-related config constants or thresholds

**Section 2 — Online Blocks 1–9**

For EACH block (B1 through B9), extract in this format:

```
### Block N: [Name]
- **PG ID:** P3-ON-BN (or whatever ID the spec assigns)
- **Purpose:** (one line)
- **Trigger:** (what starts this block — event, schedule, upstream block)
- **Inputs:**
  - Datasets: [QuestDB tables read, with key fields]
  - Redis keys: [channels/keys consumed]
  - Upstream: [which block(s) feed into this]
- **Outputs:**
  - Datasets: [QuestDB tables written, with key fields]
  - Redis keys: [channels/keys published]
  - Downstream: [which block(s) consume output]
- **Key Logic:** (summarised — formulas, thresholds, decision points)
- **Shared vs Per-User:** (does this block run once globally or per-user?)
- **Error Handling:** (what happens on failure — retry, skip, alert?)
```

### Output Instructions

1. Create the file `docs/audit/spec_reference.md` with this structure:

```markdown
# Captain System — Consolidated Spec Reference

Generated by Phase 2 Spec Extraction (2026-04-08)
Source priority: V3 repo specs supersede Obsidian originals.

---

## §1 — Session Definitions

[Your extracted content here]

---

## §2 — Online Blocks 1–9

[Your extracted content here]

---

## Session 1 Passover

- **Docs read:** Program3_Online.md (V3), 33_P3_Online_Full_Pseudocode.md, 30_P1_Consolidated_Config.md
- **Sections written:** §1, §2
- **Key spec requirements found:** [list 3-5 most critical requirements]
- **Items for next sessions:** [anything referenced but not yet extracted — AIM details, Offline blocks, etc.]
```

2. Verify: after writing, read back the file and confirm both sections are populated.

### Rules

- Extract what the spec SAYS, not what the code DOES — this is spec extraction, not code audit
- Include exact thresholds, formulas, and enum values when the spec provides them
- If the spec is ambiguous on a point, note it as `[SPEC AMBIGUOUS: ...]`
- If the Obsidian doc contains detail absent from V3, include it and tag as `[SOURCE: Obsidian doc NN]`
- Do NOT read any code files — only the spec documents listed above

---

## SPEC-EXEC-02 — AIM System Spec

---

You are extracting spec content for the Captain System consolidated spec reference. Session 2 of 7.

**Your role:** Read the AIM-related spec documents, extract per-AIM structured data, and append §3 to the existing `docs/audit/spec_reference.md`.

**Source priority:** V3 repo specs SUPERSEDE Obsidian vault originals. The V3 AIM_Extractions.md is 4.9x longer than the Obsidian equivalent — it is the authoritative source.

### Documents to Read

**PRIMARY (V3 repo — authoritative):**
- `docs/AIM-Specs/new-aim-specs/AIM_Extractions.md` (3,723 lines) — Detailed AIM paper extractions with formulas and thresholds
- `docs/AIM-Specs/new-aim-specs/HMM_Opportunity_Regime_Spec.md` (578 lines) — AIM-16 HMM regime spec

**SUPPLEMENTARY (Obsidian vault):**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/31_AIM_Individual_Specifications.md` (767 lines) — Per-AIM specs
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/22_HMM_Opportunity_Regime.md` (151 lines) — HMM overview

### What to Extract

**Section 3 — AIM System (AIMs 1–16)**

First, extract the AIM system overview:
- Total AIM count and tier structure (Tier 1, Tier 2, Tier 3)
- AIM aggregation method (DMA/MoE meta-learning)
- Lifecycle states (e.g., COLD, WARMING, WARM, ACTIVE, DECAYED)
- HDWM (seed type taxonomy) overview

Then for EACH AIM (AIM-1 through AIM-16), extract:

```
### AIM-N: [Name]
- **Tier:** [1/2/3]
- **Paper/Source:** [academic paper or data source]
- **Data Sources:** [what market data this AIM consumes]
- **Modifier Output:** [what the AIM produces — modifier value, range, interpretation]
- **Thresholds:** [key thresholds, cutoffs, or decision boundaries]
- **Warm-Up Requirements:** [minimum data/time before AIM is active]
- **QuestDB Datasets:** [tables read/written, key fields]
- **Lifecycle:** [states this AIM transitions through]
- **HDWM Seed Type:** [how this AIM is categorized for diversity maintenance]
- **Key Formula:** [primary formula/calculation, if spec provides it]
```

Special attention for AIM-16 (HMM):
- HMM state count and transition logic
- Budget allocation weights per regime state
- Cold-start blending formula
- Integration with session budgeting (Block 5 Offline)

### Output Instructions

1. Read the existing `docs/audit/spec_reference.md` (has §1–2 from Session 1)
2. Append §3 after §2, maintaining the same markdown structure
3. Add a Session 2 Passover block at the bottom:

```markdown
---

## §3 — AIM System (AIMs 1–16)

[Your extracted content here]

---

## Session 2 Passover

- **Docs read:** AIM_Extractions.md (V3), HMM_Opportunity_Regime_Spec.md (V3), 31_AIM_Individual_Specifications.md, 22_HMM_Opportunity_Regime.md
- **Sections written:** §3
- **Key spec requirements found:** [list 3-5 most critical AIM requirements]
- **AIM count confirmed:** [N AIMs extracted]
- **Items for next sessions:** [anything referenced but not yet extracted]
```

### Rules

- Extract what the spec SAYS, not what the code DOES
- Include exact formulas when the spec provides them (use LaTeX-style notation if helpful)
- The V3 AIM_Extractions.md is large (3,723L) — read it in chunks if needed, but extract ALL 16 AIMs
- If an AIM is mentioned but poorly specified, note it as `[UNDERSPECIFIED: ...]`
- Tag Obsidian-only content as `[SOURCE: Obsidian doc NN]`
- Do NOT read any code files

---

## SPEC-EXEC-03 — Offline Pipeline + Kelly + Circuit Breaker

---

You are extracting spec content for the Captain System consolidated spec reference. Session 3 of 7.

**Your role:** Read the Offline pipeline specs, extract block structure plus Kelly and Circuit Breaker details, and append §4–6 to the existing `docs/audit/spec_reference.md`.

**Source priority:** V3 repo specs SUPERSEDE Obsidian vault originals. The V3 Program3_Offline.md is 2.2x longer than the Obsidian equivalent.

### Documents to Read

**PRIMARY (V3 repo — authoritative):**
- `docs/AIM-Specs/new-aim-specs/Program3_Offline.md` (1,729 lines) — Full V3 Offline pipeline spec
- `docs/AIM-Specs/new-aim-specs/DMA_MoE_Implementation_Guide.md` (339 lines) — DMA/MoE meta-learning guide

**SUPPLEMENTARY (Obsidian vault):**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/32_P3_Offline_Full_Pseudocode.md` (782 lines) — Offline pseudocode
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/21_Implementation_Guides.md` (205 lines) — DMA/MoE, BOCPD, Kelly implementation guides

### What to Extract

**Section 4 — Offline Blocks 1–9**

For EACH block (B1 through B9), extract in this format:

```
### Block N: [Name]
- **PG ID:** P3-OFF-BN
- **Purpose:** (one line)
- **Trigger:** (event-driven, scheduled, or upstream dependency)
- **Inputs:**
  - Datasets: [QuestDB tables read, with key fields]
  - Redis keys: [channels/keys consumed]
  - Upstream: [block dependencies]
- **Outputs:**
  - Datasets: [QuestDB tables written, with key fields]
  - Redis keys: [channels/keys published]
  - Downstream: [who consumes output]
- **Key Logic:** (summarised — algorithms, formulas, thresholds)
- **Shared vs Per-User:** (global or per-user execution?)
- **Error Handling:** (failure mode)
```

**Section 5 — Kelly 7-Layer Pipeline**

Extract the COMPLETE Kelly sizing pipeline, layer by layer:

```
### Layer N: [Name]
- **Purpose:** (one line)
- **Inputs:** [datasets, parameters]
- **Formula:** [exact formula from spec]
- **Output:** [what this layer produces]
- **Dataset Refs:** [QuestDB tables involved — D12, etc.]
- **Constraints:** [caps, floors, bounds]
```

Include: raw Kelly fraction, shrinkage, regime adjustment, circuit breaker interaction, basket allocation, account-level cap, final position size.

**Section 6 — Circuit Breaker 5 Layers**

Extract ALL circuit breaker layers:

```
### Layer N: [Name]
- **Condition:** [what triggers this layer]
- **Action:** [what happens — reduce size, pause trading, alert, etc.]
- **Dataset Refs:** [QuestDB tables — D25, etc.]
- **Parameters:** [thresholds, beta_b, correlation windows]
- **Recovery:** [how trading resumes after trigger]
```

Include: per-asset vs account-level vs system-level breaker distinctions.

### Output Instructions

1. Read the existing `docs/audit/spec_reference.md` (has §1–3 from Sessions 1–2)
2. Append §4, §5, §6 after §3
3. Add Session 3 Passover:

```markdown
---

## Session 3 Passover

- **Docs read:** Program3_Offline.md (V3), DMA_MoE_Implementation_Guide.md (V3), 32_P3_Offline_Full_Pseudocode.md, 21_Implementation_Guides.md
- **Sections written:** §4, §5, §6
- **Key spec requirements found:** [list 3-5 critical Offline/Kelly/CB requirements]
- **Kelly layers confirmed:** [N layers extracted]
- **CB layers confirmed:** [N layers extracted]
- **Items for next sessions:** [anything referenced but not yet extracted]
```

### Rules

- Extract what the spec SAYS, not what the code DOES
- Include exact formulas for Kelly and CB — these are the most audit-critical sections
- Note where DMA_MoE_Implementation_Guide.md adds detail beyond the main Offline spec
- Tag Obsidian-only content as `[SOURCE: Obsidian doc NN]`
- Do NOT read any code files

---

## SPEC-EXEC-04 — Command Pipeline + GUI

---

You are extracting spec content for the Captain System consolidated spec reference. Session 4 of 7.

**Your role:** Read the Command pipeline and GUI specs, extract block structure and GUI panel definitions, and append §7–8 to the existing `docs/audit/spec_reference.md`.

**Source priority:** No dedicated V3 repo doc exists for Command — Obsidian vault docs are PRIMARY for this session. Use V3 Nomaan_Edits_P3.md for any Command amendments.

### Documents to Read

**PRIMARY (Obsidian vault — no V3 Command equivalent exists):**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/34_P3_Command_Full_Pseudocode.md` (380 lines) — Command pseudocode
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/18_GUI_Dashboard.md` (149 lines) — GUI panel spec
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/26_Notification_System.md` (159 lines) — Notification routing spec

**SUPPLEMENTARY (V3 repo — amendments only):**
- `docs/AIM-Specs/new-aim-specs/Nomaan_Edits_P3.md` (275 lines) — Command amendments and edits

**ADDITIONAL CONTEXT (if needed for user management):**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/19_User_Management.md` (158 lines) — RBAC spec

### What to Extract

**Section 7 — Command Blocks 1–10**

For EACH block (B1 through B10), extract:

```
### Block N: [Name]
- **PG ID:** P3-CMD-BN
- **Purpose:** (one line)
- **Trigger:** (always-on, event-driven, API call)
- **Inputs:**
  - Datasets: [QuestDB tables read]
  - Redis keys: [channels consumed]
  - API endpoints: [if block exposes/consumes HTTP endpoints]
- **Outputs:**
  - Datasets: [QuestDB tables written]
  - Redis keys: [channels published]
  - API responses: [endpoint responses]
- **Key Logic:** (routing rules, TAKEN/SKIPPED logic, parity filter, etc.)
- **Shared vs Per-User:** (per-user routing or system-wide?)
```

**Section 8 — GUI Panels + Security**

Extract:
- All GUI panels/tabs defined in spec (name, purpose, data sources)
- Autonomy tiers (manual, semi-auto, full-auto) — what each tier controls
- Security model: "6 outbound fields only" rule — what are the 6 fields?
- WebSocket channels for real-time GUI updates
- Notification system: 26 event types, priority levels (CRITICAL/HIGH/MEDIUM/LOW), quiet hours logic
- Telegram bot integration points

### Output Instructions

1. Read the existing `docs/audit/spec_reference.md` (has §1–6 from Sessions 1–3)
2. Append §7, §8 after §6
3. Add Session 4 Passover:

```markdown
---

## Session 4 Passover

- **Docs read:** 34_P3_Command_Full_Pseudocode.md, 18_GUI_Dashboard.md, 26_Notification_System.md, Nomaan_Edits_P3.md (V3)
- **Sections written:** §7, §8
- **Key spec requirements found:** [list 3-5 critical Command/GUI requirements]
- **Command blocks confirmed:** [N blocks extracted]
- **GUI panels confirmed:** [N panels extracted]
- **Items for next sessions:** [anything referenced but not yet extracted]
```

### Rules

- Extract what the spec SAYS, not what the code DOES
- Note that Command has no dedicated V3 doc — flag any areas where the original spec may be outdated vs V3 amendments
- If Nomaan_Edits_P3.md contradicts the Obsidian doc, prefer Nomaan_Edits and tag as `[V3 AMENDMENT]`
- Pay special attention to the security model — the "6 outbound fields" rule is critical
- Do NOT read any code files

---

## SPEC-EXEC-05 — QuestDB Schemas + Supporting Systems

---

You are extracting spec content for the Captain System consolidated spec reference. Session 5 of 7.

**Your role:** Read the QuestDB schema spec and supporting system specs, extract the complete dataset catalog and supporting system definitions, and append §9–10 to the existing `docs/audit/spec_reference.md`.

**Source priority:** V3 P3_Dataset_Schemas.md supersedes Obsidian doc 24 for schema definitions. Other supporting specs are Obsidian-only.

### Documents to Read

**PRIMARY (V3 repo for schemas):**
- `docs/AIM-Specs/new-aim-specs/P3_Dataset_Schemas.md` (565 lines) — V3 field-level QuestDB schemas

**PRIMARY (Obsidian vault for supporting systems):**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/24_P3_Dataset_Schemas.md` (265 lines) — Original schema spec (supplementary to V3)
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/25_Fee_Payout_System.md` (143 lines) — Fee resolution
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/20_Signal_Distribution.md` (108 lines) — Signal distribution / anti-copy
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/27_Contract_Rollover.md` (79 lines) — Futures contract rollover
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/28_Pseudotrader_System.md` (121 lines) — Account-aware pseudotrader

### What to Extract

**Section 9 — QuestDB Dataset Master List (D00–D27)**

For EVERY P3 dataset (D00 through D27), extract:

```
### D[NN]: [table_name]
- **Purpose:** (one line)
- **Key Fields:** [field names and types — from V3 spec]
- **Writer:** [which process/block writes to this table]
- **Reader:** [which process/block reads from this table]
- **Write Frequency:** [per-tick, per-session, daily, on-event]
- **Retention:** [if specified — partition, TTL]
- **Notes:** [special constraints, multi-user implications]
```

If a dataset is referenced in earlier sections (§1–8) but not defined in the schema docs, note it as `[REFERENCED BUT UNDEFINED]`.

**Section 10 — Supporting Systems**

Extract each supporting system:

**Fee Resolution System:**
- Fee calculation formula
- Per-asset fee structure
- Payout rules and thresholds
- TopstepX-specific fee handling

**Signal Distribution:**
- Signal format and fields
- Distribution channel (Redis)
- Anti-copy measures
- Multi-user signal routing

**Contract Rollover:**
- Rollover detection logic
- Contract ID resolution
- Timeline (when rollover happens per asset)
- Data continuity handling

**Pseudotrader System:**
- Account-aware simulation logic
- Shadow position tracking
- Theoretical outcome calculation
- Multi-instance parity interaction

### Output Instructions

1. Read the existing `docs/audit/spec_reference.md` (has §1–8 from Sessions 1–4)
2. Append §9, §10 after §8
3. Add Session 5 Passover:

```markdown
---

## Session 5 Passover

- **Docs read:** P3_Dataset_Schemas.md (V3), 24_P3_Dataset_Schemas.md, 25_Fee_Payout_System.md, 20_Signal_Distribution.md, 27_Contract_Rollover.md, 28_Pseudotrader_System.md
- **Sections written:** §9, §10
- **Datasets cataloged:** [N datasets extracted out of 28 (D00–D27)]
- **Key spec requirements found:** [list 3-5 critical schema/supporting requirements]
- **Items for next sessions:** [anything referenced but not yet extracted]
```

### Rules

- Extract what the spec SAYS, not what the code DOES
- Be exhaustive for §9 — every dataset the spec mentions must appear, even if sparsely defined
- V3 P3_Dataset_Schemas.md has field-level detail that Obsidian doc 24 lacks — prefer V3 field definitions
- If a dataset is mentioned in passing but has no formal schema, include it with `[SCHEMA INCOMPLETE]`
- Do NOT read any code files

---

## SPEC-EXEC-06 — Canvas Files + Lifecycle + Strategy Types

---

You are extracting spec content for the Captain System consolidated spec reference. Session 6 of 7.

**Your role:** Read the canvas files (JSON), lifecycle docs, and strategy type registry. Extract feedback loops, daily lifecycle, and strategy definitions. Append §11–13 to the existing `docs/audit/spec_reference.md`.

**Source priority:** Canvas files are structural (JSON with nodes and edges). V3 Cross_Reference is supplementary for lifecycle understanding.

### Documents to Read

**Canvas files (JSON — read and parse the node/edge structure):**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Functional/Programs 1-2-3.canvas` — System flow across all 3 processes
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Functional/AIM System.canvas` — AIM data flow
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Backend/VALIDATE Programs 1-2-3.canvas` — Validation flow

**Obsidian vault docs:**
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/15_Model_Definitions.md` (188 lines) — Model definitions (M-001–M-070)
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/16_Strategy_Type_Registry.md` (42 lines) — Strategy types ST-01–ST-06
- `/mnt/c/Users/nomaa/Documents/Quant_Project/Direct Information/29_Operational_Policies.md` (199 lines) — Governance, lifecycle, change management

**V3 supplementary:**
- `docs/AIM-Specs/new-aim-specs/Cross_Reference_PreDeploy_vs_V3.md` (367 lines) — V3 delta mapping (lifecycle changes)

### What to Extract

**Section 11 — Feedback Loops (all 6)**

From the canvas files, identify ALL feedback loops in the system. For each:

```
### Loop N: [Name]
- **Trigger:** [what initiates the loop — trade outcome, EOD, decay detection, etc.]
- **Data Flow:** [step-by-step: which process/block produces -> which consumes -> what updates]
- **Datasets Involved:** [QuestDB tables read/written in this loop]
- **Latency:** [real-time, per-session, daily, event-driven]
- **Cross-Process:** [which processes participate — Online, Offline, Command]
```

The 6 feedback loops to look for (from the canvas):
1. Trade outcome -> Offline learning (DMA, EWMA, Kelly, BOCPD)
2. Decay detection -> Strategy injection
3. AIM lifecycle state changes -> Online aggregation weights
4. Circuit breaker trigger -> Position sizing reduction
5. Reconciliation -> Account state update
6. Pseudotrader/shadow -> Theoretical outcome learning

**Section 12 — Daily Lifecycle**

Extract the complete daily timeline:

```
### Daily Lifecycle Timeline (all times ET)
- **19:00** — SOD reset (what resets, what persists)
- **Pre-session** — what runs before session open
- **Session open** — OR calculation, data ingestion start
- **OR complete** — signal generation begins
- **Trading window** — active monitoring, position management
- **Session close** — EOD procedures
- **Post-session** — reconciliation, learning updates
```

Include: what happens on weekends, holidays, contract rollover days.

**Section 13 — Strategy Types + Exit Grid**

Extract:
- Strategy types ST-01 through ST-06 (or however many the spec defines)
- Per-type: entry logic, exit grid, variant count
- Exit grid structure: TP levels, SL levels, trailing stop logic
- How strategy type maps to (m,k) pairs from P2

### Output Instructions

1. Read the existing `docs/audit/spec_reference.md` (has §1–10 from Sessions 1–5)
2. Append §11, §12, §13 after §10
3. Add Session 6 Passover:

```markdown
---

## Session 6 Passover

- **Docs read:** Programs 1-2-3.canvas, AIM System.canvas, VALIDATE Programs 1-2-3.canvas, 15_Model_Definitions.md, 16_Strategy_Type_Registry.md, 29_Operational_Policies.md, Cross_Reference_PreDeploy_vs_V3.md (V3)
- **Sections written:** §11, §12, §13
- **Feedback loops confirmed:** [N loops extracted]
- **Strategy types confirmed:** [N types extracted]
- **Key spec requirements found:** [list 3-5 critical lifecycle/strategy requirements]
- **CHECKPOINT NOTE:** All 13 sections should now be complete. Review full spec_reference.md before EXEC-07.
```

### Canvas File Parsing Notes

Canvas files are JSON with this structure:
```json
{
  "nodes": [{"id": "...", "type": "text", "text": "...", "x": N, "y": N, ...}],
  "edges": [{"id": "...", "fromNode": "...", "toNode": "...", "label": "..."}]
}
```

Extract: node labels (the text content), edge labels (data flow), and the overall flow topology. The spatial layout (x,y) encodes grouping — nodes close together are related.

### Rules

- Extract what the spec SAYS, not what the code DOES
- Canvas files require structural parsing — don't just dump the JSON, interpret the flow
- If a feedback loop is implied by the canvas but not explicitly labeled, include it as `[INFERRED FROM CANVAS]`
- Cross-reference the canvas flows against the block definitions from §2, §4, §7 — note any inconsistencies
- Do NOT read any code files

---

## SPEC-EXEC-07 — Master Gap Analysis (FINAL)

---

You are performing the Master Gap Analysis for the Captain System. Session 7 of 7 — FINAL session.

**Your role:** Cross-reference the completed spec reference (13 sections) against Phase 1 code audit reports AND the live codebase. Produce a comprehensive gap analysis documenting every divergence between spec and implementation.

**CRITICAL:** This session runs LAST, after all 6 prior sessions have completed and the spec_reference.md has been reviewed at checkpoint.

### Documents to Read

**Spec Reference (completed by Sessions 1–6):**
- `docs/audit/spec_reference.md` — All 13 sections (§1–13)

**Phase 1 Code Audit Reports:**
- `docs/audit/captain_online.md` (1,457 lines) — Online pipeline audit
- `docs/audit/captain_offline.md` (804 lines) — Offline pipeline audit
- `docs/audit/captain_command.md` (930 lines) — Command interface audit
- `docs/audit/cross_cutting.md` (682 lines) — Shared library/config audit

### What to Produce

Write `docs/audit/master_gap_analysis.md` with this structure:

#### Header

```markdown
# Captain System — Master Gap Analysis

Generated: [date]
Spec source: docs/audit/spec_reference.md (13 sections)
Code audit source: docs/audit/ (4 Phase 1 reports)
Methodology: Spec-to-code cross-reference with codebase verification
```

#### Part 1 — Summary Statistics

```
| Metric | Count |
|--------|-------|
| Total spec components analyzed | N |
| Aligned (code matches spec) | N |
| Divergent (code differs from spec) | N |
| Stubbed (placeholder/TODO in code) | N |
| Missing (spec requires, code absent) | N |
```

#### Part 2 — Critical Gaps (blocking live multi-session trading)

For EACH gap, use this format:

```
### G-XXX: [Gap Title]
- **Severity:** CRITICAL / HIGH / MEDIUM / LOW
- **Spec Ref:** [doc:section:PG ID] — what the spec requires
- **Code Ref:** [file:line] — what the code actually does (or "ABSENT")
- **Gap:** [one-line description of the divergence]
- **Impact:** [what breaks if this isn't fixed — session trading, learning loop, risk, etc.]
- **Dependencies:** [other gaps that must be fixed first or together]
- **Complexity:** S / M / L / XL
```

#### Part 3 — Gap Sections

Organize gaps into these sections:
1. **Online Pipeline Gaps** (by block, B1–B9)
2. **Offline Pipeline Gaps** (by block, B1–B9)
3. **Command Pipeline Gaps** (by block, B1–B10)
4. **Session/Trigger Gaps** (multi-session support, session_match, timing)
5. **QuestDB Schema Gaps** (missing tables, wrong fields, missing datasets)
6. **AIM Implementation Gaps** (per-AIM: spec vs code divergences)
7. **Kelly/Circuit Breaker Gaps** (layer-by-layer comparison)
8. **Feedback Loop Gaps** (which of the 6 loops are broken/missing)
9. **GUI/Security Gaps** (panel coverage, security model compliance)

#### Part 4 — Recommended Implementation Order

Dependency-sorted, critical-first:

```
| Priority | Gap IDs | Description | Blocked By | Complexity |
|----------|---------|-------------|------------|------------|
| 1 | G-001, G-002 | ... | — | M |
| 2 | G-005 | ... | G-001 | L |
| ... | | | | |
```

### How to Find Gaps

For each spec component in spec_reference.md:
1. Check if Phase 1 audit already identified a related finding — cite it
2. If Phase 1 didn't cover it, search the codebase yourself:
   - Use Grep to find the relevant function/class/block implementation
   - Compare what the code does against what the spec requires
   - Note the exact file and line number
3. Categorize: ALIGNED, DIVERGENT, STUBBED, or MISSING

### Audit Skills Available

You may invoke these audit skills for deeper analysis if needed:
- `/codebase-audit-suite:ln-620-codebase-auditor` — General codebase audit coordinator
- `/codebase-audit-suite:ln-623-code-principles-auditor` — DRY, KISS, error handling
- `/codebase-audit-suite:ln-627-observability-auditor` — Logging, metrics, tracing
- `/codebase-audit-suite:ln-629-lifecycle-auditor` — Bootstrap, shutdown, signal handling

### Rules

- Every gap MUST have BOTH a spec citation (doc:section) AND a code citation (file:line or "ABSENT")
- Do NOT propose fixes — only document gaps. Fixes come in Phase 3.
- Use Phase 1 audit findings as starting points, but verify against the now-complete spec reference
- Phase 1 audits may have found code issues not related to spec compliance — only include gaps that represent spec-vs-code divergence
- Be exhaustive — it's better to document a minor gap than to miss a critical one
- Gap IDs: use G-001 through G-NNN, numbered sequentially within each section

---

## Docs Not Assigned to Any Session

The following Obsidian vault docs are NOT read by any executor session. They cover P1/P2 topics outside the P3 Captain System scope:

| Doc # | File | Lines | Reason Excluded |
|-------|------|-------|-----------------|
| 00 | 00_GAP_ANALYSIS.md | 457 | Index doc (already used for planning) |
| 01 | 01_DIAGRAM_CONVENTIONS.md | 85 | Visual conventions only |
| 12 | 12_Input_Variables.md | 158 | P1 feature definitions (not P3 scope) |
| 13 | 13_Transformations.md | 87 | P1 transformations (not P3 scope) |
| 14 | 14_Binary_Events.md | 81 | P1 binary events (not P3 scope) |
| 17 | 17_Sample_Periods_Per_Asset.md | 176 | P1 sample periods (not P3 scope) |
| 19 | 19_User_Management.md | 158 | May be read by EXEC-04 if needed for RBAC context |
| 23 | 23_XGBoost_Classifier.md | 156 | P1/P2 classifier (not directly P3 — but may be AIM-related) |

**V3 repo docs not assigned:**

| File | Lines | Reason |
|------|-------|--------|
| CaptainNotes.md | 563 | Dev notes — useful context but not spec content |
| AIM_Extractions.md | 3,723 | Assigned to EXEC-02 |

If any executor discovers references to these docs during extraction, they should note the reference in their passover for potential patch sessions.

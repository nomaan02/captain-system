# Agent C — Executor Session 05: Timezone + Session Infrastructure

You are AGENT C (Executor). You implement code changes.
You do NOT plan future sessions or make architectural decisions.

## Previous Session Passover
Session 04 Passover
Items completed: G-002, G-003, G-021, G-055, G-020, G-089, G-056 (all 7)
Items remaining: none
New findings:
  - G-NEW-012: captain-gui Dockerfile runs non-root but /gui-dist is a named Docker volume. On existing deployments where the volume was created as root, the non-root user will fail to write. Fix: `docker compose down -v` to recreate volumes, or add an entrypoint that chowns the mount.
  - G-NEW-013: api.py str(exc) patterns remain in b11_replay_runner.py (lines 184, 193, 198, 536), b9_incident_response.py (lines 189, 257), b8_reconciliation.py (line 85), b5_injection_flow.py (line 146), b3_api_adapter.py (lines 189, 274). These are internal block return values, not direct HTTP responses — lower priority than the api.py/b6 fixes but should be reviewed in a future session.
Decisions still needed: none
Test status: 95 passed / 0 failed
Audit skill results: pending (ln-621, ln-643 to be run post-session)
Notes:
  - G-002: JWT middleware uses BaseHTTPMiddleware. Exempt paths: /api/health, /api/status, /auth/token, /docs, /openapi.json, /redoc. WebSocket bypasses middleware (auth handled in endpoint). JWT_SECRET_KEY and API_SECRET_KEY env vars required — add to .env before next deploy. If JWT_SECRET_KEY not set, ephemeral key generated (tokens won't persist across restarts).
  - G-003: Entire git-pull endpoint + _rebuild helper deleted (108 lines). The documented update path is `captain-update.sh` from the host.
  - G-021: QuestDB uses $N positional parameters (psycopg2 wire protocol). The role list is passed as a Python list to cur.execute().
  - G-055: WebSocket connects via /ws/{user_id}?token=<jwt>. Token sub claim must match path user_id. Close code 4003 used for auth failures (before accept()).
  - G-020: Docker CLI (docker-27.5.1), Compose plugin (v2.35.1), git, /captain/repo bind mount, and /var/run/docker.sock all removed. git removed from apt-get install since it was only needed for the git-pull feature.
  - G-089: All 4 Dockerfiles now create appuser with UID 1000 (matches typical WSL2 host user). captain-command also creates /captain/logs and /captain/vault-backup directories. captain-gui creates /gui-dist pre-volume-mount for Docker volume initialization.
  - G-056: 12 str(exc) replaced in api.py (10 replay endpoints, 1 telegram history, 1 git-pull already removed). 2 str(exc) replaced in b6_reports.py (RPT-01, RPT-07). All replacements use generic messages; original exceptions logged at ERROR level.
  - Matrix §6 Dashboard updated: UNRESOLVED 49->42, FIXED 12->19.
  - Matrix §7 Changelog populated with all 7 fixes.
  - New env vars needed in .env: API_SECRET_KEY, JWT_SECRET_KEY, JWT_EXPIRY_HOURS (optional, defaults to 24).
  - GUI frontend will need updating to: (1) call POST /auth/token to get JWT, (2) send Authorization: Bearer header on all API calls, (3) pass ?token= query param on WebSocket connections.

## Resolved Decisions
Check matrix for DEC-07 (ZN/ZB session mapping) resolution.

Read `plans/CAPTAIN_RECONCILIATION_MATRIX.md` in full before starting.

## Assignment (in order)

### 1. G-024 | HIGH | Offline Scheduler Wrong Timezone
**File:** captain-offline/captain_offline/orchestrator.py
**Line:** 529
**Fix:** Replace `datetime.now()` with `datetime.now(ZoneInfo("America/New_York"))`. Import ZoneInfo from zoneinfo stdlib.
**Spec:** §1 REQ-4 — timezone is always America/New_York

### 2. G-029 | MEDIUM | Online datetime.now() Without ET
**Files:** captain-online/captain_online/blocks/b1_data_ingestion.py:436,642; b1_features.py:546
**Fix:** Replace all `datetime.now()` with ET-aware version in these 3 locations.
**Spec:** §1 REQ-4

### 3. G-051 | MEDIUM | Offline datetime.now() Throughout
**Files:** ~20 sites across captain-offline orchestrator + B3-B9
**Fix:** Grep for `datetime.now()` in captain-offline/ and replace with ET-aware version. Consider creating a helper `now_et()` in shared/constants.py if not already present.
**Spec:** §1 REQ-4
**Deps:** G-024

### 4. G-036 | MEDIUM | Session Open Time Always 9:30
**File:** captain-online/captain_online/blocks/b1_features.py
**Lines:** 1073-1082
**Fix:** `_get_session_open_time()` must return per-session open times: NY=09:30, LON=03:00, APAC=18:00 ET. Read from session_registry.json or constants.
**Spec:** §1 per-session open times
**Deps:** G-017 (session names fixed)

### 5. G-065 | MEDIUM | ZN/ZB Session Mapping Conflict
**File:** config/session_registry.json
**Fix:** Per DEC-07 resolution. If UNRESOLVED, flag and skip.
**Spec:** §1 session mapping
**Deps:** DEC-07

### 6. G-007 | HIGH | AIM-03 GEX Hardcoded Multiplier
**File:** captain-online/captain_online/blocks/b1_features.py
**Lines:** 955-956
**Fix:** Replace hardcoded `50.0` (ES multiplier) with per-asset contract multiplier from D00 (`p3_d00_asset_universe`). Query the `contract_multiplier` column.
**Spec:** §3 AIM-03 — per-asset contract multiplier from D00

## Post-Fix
1. Run `/ln-647-env-config-auditor` — timezone and config alignment
2. Run `/ln-641-pattern-analyzer` — session_match compliance

## Rules
1. ONE ITEM AT A TIME. After each: show diff, run pytest, update matrix.
2. If DEC-07 is UNRESOLVED, skip G-065 and flag.
3. New issues → G-NEW-XXX, flag, do NOT fix.
4. After all items: update §6 Dashboard, git commit.

## Passover
Write to `plans/prompts/.passovers/session_05.txt`:
```
Session 05 Passover
Items completed: [list]
Items remaining: [list or "none"]
New findings: [list or "none"]
Test status: [pass/fail count]
Audit skill results: [summary]
Notes: [anything for next session]
```

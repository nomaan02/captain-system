# Bootstrap State — Session B

**Date:** 2026-04-20
**Branch:** migration/fresh-start-bootstrap
**Status:** READY FOR SESSION C (verification tooling)

---

## Files Changed This Session

### Application fixes (C-4 and C-2 from session A audit)

**`captain-online/captain_online/blocks/b7_position_monitor.py`**
- C-4 fix: `_update_capital_and_cb()` now reads all 12 non-derived D16 columns
  (`status, role, starting_capital, total_capital, accounts, max_simultaneous_positions,
  max_portfolio_risk_pct, correlation_threshold, user_kelly_ceiling, capital_history,
  telegram_chat_id, created`) and writes them all back on update.
- Before: only wrote 4 cols `(user_id, total_capital, accounts, last_updated)`, which
  nulled out 10 cols on every P&L update under DEDUP UPSERT KEYS.
- Pattern: read-modify-write (SELECT all → compute new_capital → INSERT all).

**`captain-offline/captain_offline/blocks/b7_tsm_simulation.py`**
- C-2 fix: the Monte Carlo simulation writer now does read-modify-write on D08.
- Before: wrote only 4 cols `(account_id, pass_probability, simulation_date, risk_goal,
  last_updated)`, creating a 28-NULL row that becomes LATEST ON after each simulation run.
- Now: reads all 28 non-derived D08 cols from the existing row, updates only
  `pass_probability`, `simulation_date`, `risk_goal`, writes all 32 cols back.
- If no D08 row exists yet (TSM config not loaded), logs a warning and skips — safe
  because b4_tsm_manager always loads the full TSM config before b7_simulation runs.

### Script rewrites

**`scripts/init_questdb.py`** (818 lines → 37 lines)
- Replaced all hand-written DDL with `from shared.canonical_schemas import CANONICAL_DDLS`.
- Iterates CANONICAL_DDLS, executes each DDL with error reporting per table.
- Kept as standalone script for direct container invocation.

**`scripts/init_all.py`**
- Step 2 (QuestDB schema) now imports `CANONICAL_DDLS` and `table_name_of` directly
  from `shared.canonical_schemas` — no hand-written DDL anywhere in the install path.
- All other steps unchanged (SQLite journals, params, compliance gate, test seed,
  verification summary).

**`scripts/compact_questdb_tables.py`**
- D02 DDL drift fixed: was using the wrong 7-col schema (`weight, dma_score, trend,
  anomaly_flag, override_reason, ...`). Now imports `D02_AIM_META_WEIGHTS` from
  `shared.canonical_schemas` (correct 6-data-col schema: `inclusion_probability,
  inclusion_flag, recent_effectiveness, days_below_threshold`).
- All 5 DDLs (D01, D02, D05, D12, D25) now imported from canonical_schemas — no more
  drift risk.
- Added `CAPTAIN_COMPACTION_ENABLED` feature-flag check at start of `main()` (belt-and-
  braces for direct script invocation; orchestrator already checks this flag before
  calling the script).

### Deleted files

**`scripts/migrate_account.py`** — removed (`git rm`).
- One-shot migration tool from legacy multi-account migration plan.
- Incompatible with fresh-start approach (assumes old schema without WAL/DEDUP).
- No active call site in any service.

---

## Phase 0 Work — Confirmed Present (no changes needed)

The following Phase 0 items are already on `migration/fresh-start-bootstrap`:

| Item | File | Status |
|------|------|--------|
| `connect_timeout=5` | `shared/questdb_client.py:45,78` | ✅ present |
| `wait_for_questdb(max_wait=30)` | `shared/questdb_client.py:68` | ✅ present |
| Health gate at startup | `captain-offline/captain_offline/main.py:105` | ✅ present |
| Health gate at startup | `captain-online/captain_online/main.py:260` | ✅ present |
| Health gate at startup | `captain-command/captain_command/main.py:45` | ✅ present |
| `CAPTAIN_COMPACTION_ENABLED` flag | offline orchestrator + compact script | ✅ present |

---

## Bootstrap Script Review — bootstrap_production.py

All four phases align with canonical column names and types. All four are
idempotent (check-before-insert pattern):

| Phase | Table | Idempotence check | Column alignment |
|-------|-------|-------------------|-----------------|
| 1 | D00 | `read_d00_row()` + `update_d00_fields()` — read-modify-write | ✅ canonical |
| 2 | D16 | `SELECT count() WHERE user_id = %s` | ✅ all 14 canonical cols |
| 3 | D02 | `SELECT count() >= expected` | ✅ all 7 canonical cols |
| 4 | D25 | `SELECT count() WHERE account_id = %s` | ✅ all 11 canonical cols |

No changes to bootstrap_production.py — it was already correct.

---

## Deferred Items from .schema-audit.md

The following remain unfixed in this session (application code patches required):

### A-1 — `p3_d21_incident_log` column name mismatch
- **File:** `captain-online/captain_online/blocks/b1_data_ingestion.py:692`
- **Problem:** INSERT writes `ts` column; canonical DDL uses `timestamp` (majority of
  5 sites use `timestamp`).
- **Impact:** This INSERT will fail under canonical DDL. The table was not WAL/DEDUP
  before, so it has not been affecting production yet. Fix before deploying to tower.
- **Fix:** Change `ts` → `timestamp` at that one INSERT site.

### A-2 — `p3_d33_opening_volatility.session_date` type mismatch
- **File:** `captain-online/captain_online/blocks/b1_features.py:1278`
- **Problem:** INSERT passes a Python string `"YYYY-MM-DD"` into a `TIMESTAMP` column.
  QuestDB accepts the implicit cast but it's fragile.
- **Fix:** Cast to timestamp at the call site (`to_timestamp(today_str, 'yyyy-MM-dd')`
  or use `now()::DATE::TIMESTAMP`).

---

## DEDUP Behaviour Notes

### State tables (D01, D02, D05, D12, D25) — DEDUP is effectively a no-op at runtime

All writers for these tables use `now()` for `last_updated` (the designated timestamp),
which generates a unique timestamp on every write. Because the DEDUP key includes
`last_updated`, DEDUP never fires for these tables in normal operation.

**Consequence:** compaction is NOT belt-and-braces for these tables — it is the primary
deduplication mechanism. `CAPTAIN_COMPACTION_ENABLED=true` must remain the default until
at least two weeks of production monitoring with the new canonical DDL confirms the
behaviour is acceptable. Only after that can compaction be disabled.

### D08 TSM state — same situation

Writers use `now()` for `last_updated`. DEDUP UPSERT KEYS(last_updated, account_id)
won't collapse rows in practice. The C-2 fix (read-modify-write in b7_tsm_simulation)
is the correct mitigation — it ensures the LATEST ON row is always the full 32-col row,
regardless of DEDUP behaviour.

### D08 DEDUP key — account_id chosen

Majority of call sites query `LATEST ON last_updated PARTITION BY account_id`.
`migrate_account.py` (now deleted) used `PARTITION BY user_id` — this was
a SELECT query, not tied to the DEDUP key. LATEST ON is independent of DEDUP keys;
it scans the partition by the specified column. No other active call site uses
`PARTITION BY user_id` on D08.

---

## Next: Session C — Verification Tooling

Verify the fresh-start install end-to-end on a scratch QuestDB instance:
1. Run `init_all.py` on a clean QuestDB container — confirm all 39 tables created.
2. Run `bootstrap_production.py --dry-run` — confirm no errors.
3. Confirm D16 read-modify-write (C-4 fix) preserves all 14 cols on simulated P&L update.
4. Confirm D08 read-modify-write (C-2 fix) preserves all 32 cols on simulated simulation run.
5. Patch A-1 (`b1_data_ingestion.py:692` `ts` → `timestamp`).
6. Patch A-2 (`b1_features.py:1278` string → timestamp cast).

---

## Session D — Local validation (2026-04-21)

Ran the full a–j chain against `captain-local-qdb` (QuestDB 9.3.3 in Docker,
localhost:8812/9000, user=captain/localdev) plus a local Redis 7 container.
All scripts executed from the host venv. Actual PASS output below.

### Container-side fixes applied during validation

1. **`shared/questdb_client.py` — verify_schema_drift** used `columnName` /
   `columnType` from `table_columns(...)`; real QuestDB columns are `column`
   and `type`. Fixed in `scripts/verify_schema_drift.py` (`_live_columns`).
2. **`scripts/bootstrap_production.py` Phase 1 (D00)** — WAL eventual
   consistency: INSERT then `LATEST ON` in quick succession did not see the
   fresh row, so `update_d00_fields` crashed on a not-yet-visible MES row.
   Fixed by merging defaults + updates into a single INSERT when the asset
   does not exist, using a shared cursor.
3. **`scripts/bootstrap_production.py` Phase 2 (D16)** — init_all test-seed
   creates a zero-capital silo for `primary_user`, which caused the
   "silo already exists" skip to fire before the real bootstrap row was
   written. Skip now requires `starting_capital > 0`.

### a. `.env.local` pointed at `localhost:8812` / user `captain` / password `localdev`

(Env set inline in the shell — no `.env.local` file committed.)

### b. `init_all.py` — 39 canonical tables + SQLite + D17 + test seed

```
--- QuestDB Schema (canonical_schemas.py) ---
  [OK] p3_d00_asset_universe … [OK] p3_audit_log  (39/39)
--- Task 1.5: SQLite WAL Journals ---
  [OK] captain-offline/journal.sqlite
  [OK] captain-online/journal.sqlite
  [OK] captain-command/journal.sqlite
--- Task 1.7: System Parameters ---
  [OK] Seeded 43/43 system parameters.
--- Task 1.9: Test Asset + User Seed ---
  [OK] P3-D00: ES asset inserted (captain_status=WARM_UP)
  [OK] P3-D15: primary_user created (role=ADMIN)
  [OK] P3-D16: primary_user capital silo created

PHASE 1 INITIALIZATION SUMMARY
  QuestDB tables: 39
  System params seeded: 43
  Assets in universe: 1
  Users: 0
  Redis: connected
[DONE] Phase 1 initialization complete.
```

### c. `bootstrap_production.py` — D00 (×10), D16 silo, D02 (×60), D25 (×1)

```
  PHASE 1: D00 locked strategies + asset specs
    [OK] ES: m=7,  k=33,  OO=0.8832, pv=50.0,   margin=12650.0
    [OK] MES: m=7, k=32,  OO=0.8879, pv=5.0,    margin=1265.0
    [OK] NQ: m=3,  k=32,  OO=0.8242, pv=20.0,   margin=17600.0
    [OK] MNQ: m=5, k=32,  OO=0.8236, pv=2.0,    margin=1760.0
    [OK] M2K: m=5, k=32,  OO=0.9245, pv=5.0,    margin=700.0
    [OK] MYM: m=9, k=115, OO=0.7705, pv=0.5,    margin=880.0
    [OK] NKD: m=6, k=6,   OO=0.8533, pv=5.0,    margin=7700.0
    [OK] MGC: m=2, k=29,  OO=0.8892, pv=10.0,   margin=1000.0
    [OK] ZB:  m=10, k=113, OO=0.8054, pv=1000.0, margin=3300.0
    [OK] ZN:  m=4, k=37,  OO=0.9058, pv=1000.0, margin=2000.0
  Phase 1 complete: 10 assets updated
  PHASE 2: [OK] primary_user: capital=$150,000, accounts=[20319811], max_pos=5
  PHASE 3: [OK] 60 rows (10 assets x 6 AIMs, initial_p=0.1667)
  PHASE 4: [OK] 20319811: cold-start (beta_b=0, layers 3-4 disabled)
  Verification: PASS
```

### d. `verify_schema_drift.py`

```
PASS: all 39 canonical tables match live QuestDB schema
```

### e. `verify_bootstrap.py`

```
PASS: D00 10 assets, D16 primary_user, D02 60 rows, D25 20319811 — all seed rows confirmed
```

### f. `health_smoke_test.py`

```
1. QuestDB health gate (wait_for_questdb, max 30s)… reachable.
2. Reading one row from each critical table…
   D00 21 rows · D02 60 · D08 0 · D12 0 · D16 2 · D25 1 · D30 0
3. Scratch table write/read/dedup-replace…
   first write OK (val=1.0) · read back val=1.0
   dedup-replace OK (val=2.0) · dedup confirmed: count=1, val=2.0
   dropped p3_smoke_scratch
PASS: QuestDB reachable, 7 critical tables readable, scratch write/read/dedup/drop OK
```

### g. Seed chain (dependency order from `.seed-audit.md § 6`)

```
seed_all_assets.py         → 17/17 assets (10 ACTIVE + 1 P2_ELIM + 6 P1_ELIM)
                             D00=47 D01=120 D04=10 D05=60 D12=60
seed_ohlcv_from_qc.py      → D30 = 2,829 rows (10 assets × 283 days)
seed_iv_rv_from_extract.py → D31 = 122 rows (ES only, 2025-08-22→2026-03-27)
seed_skew_from_extract.py  → D32 =  81 rows (ES only, 2025-12-03→2026-03-31)
seed_or_volumes_from_qc.py → D29 = 240 rows (10 × 24 days)
seed_opening_vol_from_qc.py→ D33 = 240 session-rows
seed_system_params.py      → D17 = 43 params
roll_calendar_update --update
    [MGC] CONTRACT CHANGED: CON.F.US.MGC.M26 -> CON.F.US.MGC.J26
    P3-D00 roll_calendar updated for 10/10 assets
    Contract resolver cache invalidated
```

### h. Bootstrap idempotence — second run

```
PHASE 1: 10/10 [OK]     (read-modify-write preserves all cols)
PHASE 2: [SKIP] primary_user silo already bootstrapped (capital=$150,000)
PHASE 3: [SKIP] D02 already has 60 rows (need >= 60)
PHASE 4: [SKIP] 20319811 circuit breaker row already exists
Verification: PASS
```

### i. Seed chain idempotence — DEDUP behaviour as designed

| Table | DDL designated ts    | First pass | Second pass | Behaviour                             |
|-------|----------------------|------------|-------------|----------------------------------------|
| D30   | `ts` (now())         | 2,829      | 5,658       | DEDUP no-op; re-seed doubles (historical) |
| D29   | `ts` (now())         | 240        | 480         | DEDUP no-op; re-seed doubles (historical) |
| D31   | `trade_date`         | 122        | 122         | DEDUP collapses (session-date keyed)   |
| D32   | `trade_date`         | 81         | 81          | DEDUP collapses (session-date keyed)   |
| D33   | `session_date`       | 240        | 240         | DEDUP collapses (session-date keyed)   |
| D01   | `last_updated` now() | 120        | 240         | DEDUP no-op; compaction cleans up      |
| D05   | `last_updated` now() | 60         | 120         | DEDUP no-op; compaction cleans up      |
| D12   | `last_updated` now() | 60         | 120         | DEDUP no-op; compaction cleans up      |

`compact_questdb_tables.py` ran afterwards and compacted D01 (240 → 60, 4× bloat
trigger). D02 / D05 / D12 / D25 were under the 3× trigger and skipped — expected.

### j. Drift injection — fault detection

Injected `drift_canary INT` into D00 DDL in a scratch copy of
`shared/canonical_schemas.py`:

```
DRIFT (1 tables differ from canonical):
  p3_d00_asset_universe:
    absent cols (in DDL, not in DB): ['drift_canary']
FAIL: 0 missing, 1 drifted (of 39 canonical tables)
exit=1
```

Reverted the canonical file and re-verified:

```
PASS: all 39 canonical tables match live QuestDB schema
exit=0
```

### Local teardown

`captain-local-qdb` container stopped and removed; QuestDB volume under
`~/captain-local-qdb/questdb/` preserved for re-runs. Local Redis container
torn down at the same time.

**Session D local validation: PASS. Ready for tower deployment via
`bootstrap-runbook/tower-fresh-install.md`.**
